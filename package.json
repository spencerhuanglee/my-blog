{
  "name": "my-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf _site && eleventy",
    "start": "rm -rf _site && eleventy --serve",
    "new": "node scripts/new-post.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
